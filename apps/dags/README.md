# DAGS Front-End

Project-wide build, deploy, Docker, and environment configuration documentation now lives in the repository root `README.md`.

Useful local commands for this app:

```bash
pnpm dev:ui
pnpm nx run dags:build
pnpm nx run dags:test
```
